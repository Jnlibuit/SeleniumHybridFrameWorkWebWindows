package com.abc.desktop.applications.calculator.scripts;

import org.testng.annotations.Test;

import com.abc.StartWinium.StartWinium;
import com.abc.desktop.applications.calculator.reuse.StartCalculator;
 
public class Addition extends StartWinium {
	
	@Test
	public void testAddition() throws InterruptedException {
		
		//driver.quit();
		//driver.findElementByName("Seven").click();
		//StartCalculator.childTest = StartCalculator.parentTest.createNode( "Susscessfully clicked 7");
		Thread.sleep(2000);
/*
		driver.findElementById("93").click();
		StartCalculator.childTest = StartCalculator.parentTest.createNode( "Susscessfully clicked +");
		Thread.sleep(500);
		
		driver.findElementByName("8").click();
		StartCalculator.childTest = StartCalculator.parentTest.createNode( "Susscessfully clicked 8");
		Thread.sleep(500);

	*/	
	}
}
